{
  "version": 2,
  "buildCommand": "cd frontend && npm install && CI=false npm run build",
  "outputDirectory": "frontend/build",
  "installCommand": "python3 -m pip install --upgrade pip setuptools --break-system-packages && npm install --prefix backend && npm install --prefix frontend",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://panchbackend.vercel.app/api/:path*"
    },
    {
      "source": "/admin/(.*)",
      "destination": "/backend/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
